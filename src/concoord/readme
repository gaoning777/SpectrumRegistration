Instantiate Concoord Objects:
	1. update PYTHON PATH
		export PYTHONPATH=$PYTHONPATH:/home/ubuntu/Spectrum_Access/spectrum_access/src/concoord/
	2. create concoord object proxy
		concoord object -o UpdateQueue.UpdateQueue
	3. start concoord object(use IP instead of Host)
		concoord replica -o UpdateQueue.UpdateQueue -a $IP -p 14000
			or
		concoord replica -o UpdateQueue.UpdateQueue -b $RemoteIP:14000 -a $LocalIP -p 14000
	4. use the concoord object in client
		use UpdateQueueproxy.UpdateQueue
		e.g. queue=UpdateQueueproxy.UpdateQueue("10.10.10.1:14000, 10.10.10.2:14000")
